# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 저장소 성격

이것은 소프트웨어 프로젝트가 아니라 **Obsidian 볼트(vault)** 입니다. 빌드·린트·테스트 명령이 없고, 패키지 매니페스트도 없습니다. 산출물은 전부 한국어 마크다운 노트이므로, 작업은 "코드 수정"이 아니라 "노트 작성·정리·연결"입니다. `.obsidian/plugins/*/main.js`는 서드파티 플러그인 번들이므로 편집·리팩터링 대상이 아닙니다.

작성 언어는 한국어입니다. 새 노트나 노트 수정도 한국어로 합니다.

## 폴더 구조와 역할

- `_raw/교육대학원/` — 대학원 수업별 노트. 과목(`수학교육론`, `현대대수학`, `학습과학`, `특수교육학`, `학교폭력예방과 학생의 이해`) → 하위 주제(`개념정리`, `구성주의`, `준경험주의`, `수학교육운동`, `시험가이드`, `수업`, `필기노트`) 순으로 중첩됩니다. `필기노트/`는 손글씨 PDF 스캔 보관소이고(`제목_YYMMDD_HHMMSS.pdf`), 텍스트 노트는 상위 폴더에 둡니다.
- `교회/` — `설교/구약|신약/<성경책>/<책 장 절 ~ 절>.md` 형식의 설교 노트와 `셀모임/YYYY.MM.DD.md`.
- `Excalidraw/` — Excalidraw 플러그인이 생성하는 `.excalidraw.md` 도형 파일. 사람이 직접 편집하지 않습니다.
- `wiki/` — 비어 있음. 정제된 위키 노트를 두려고 만든 자리로 보이며, `_raw`(원본 강의·독서 노트)와 대비되는 이름입니다. 여기에 파일을 새로 만들 때는 사용자에게 의도를 확인하세요.
- `.trash/` — Obsidian 휴지통인데 **git에 추적되고 있습니다**. 여기 있는 파일은 삭제된 것으로 취급하고 참고·복원 용도로만 읽으세요.

`_raw`와 같은 주제가 `.trash`에도 중복 존재하는 경우가 많습니다(예: `구성주의`, `수학교육운동`). 항상 `_raw` 쪽을 정본으로 봅니다.

## 노트 작성 관례

기존 노트에서 관찰되는 형식을 따르세요.

- **YAML 프론트매터를 쓰지 않습니다.** 거의 모든 노트가 프론트매터 없이 본문으로 바로 시작합니다.
- 분류가 필요하면 파일 첫 줄에 인라인 태그를 나열합니다. 공백 대신 밑줄을 씁니다: `#지식의_자주적_구성의_원리 #지식의_생장지향성_원리`
- 제목 계층은 `##` → `####` → `#####`처럼 건너뛰는 경우가 흔하고, `### 1. …`, `#### 2) …`, `##### (1) …`처럼 번호를 제목에 직접 붙입니다. 파일명이 곧 노트 제목이라 `#` 단일 제목은 보통 생략합니다.
- 관련 노트는 **파일 맨 아래에 `[[노트명]]` 위키링크만 줄바꿈으로 나열**합니다(`[[조작적 구성주의]]` 형태). 링크는 폴더 경로 없이 노트명만 씁니다. 특정 절을 가리킬 때는 `[[#3) 주요 이론|표시할 텍스트]]` 형태의 헤딩 링크를 씁니다.
- 핵심 용어는 `**굵게**`, 부가 강조는 `*기울임*`으로 표시합니다. 정리·정의 노트에서 증명은 `> 증명.` 인용 블록 안에 넣습니다.
- 수학은 인라인 `$...$`, 별행 `$$...$$` LaTeX을 씁니다. 현대대수학 노트는 `## 1.2 Definition. Group`, `## 1.3 Theorem. Uniqueness of Identity`처럼 교재 번호 + 영문 정리명 형식을 따릅니다.
- 시험 대비 노트(`예상문제.md`, `시험가이드/`)에서는 `> **출제 의도**:` 인용과 `<details><summary>풀이 방향 (클릭)</summary>` 접기 블록을 씁니다.
- 목록 계층은 탭 들여쓰기로 깊게 내려갑니다(3~4단계). 짧은 문단보다 중첩 목록을 선호하는 스타일입니다.

## Obsidian 설정상 주의점

- `newFileLocation: root`, `attachmentFolderPath: /` — 새 노트와 첨부 이미지는 기본적으로 볼트 루트에 생깁니다. 루트에 `Pasted image *.jpg` 같은 파일이 보이면 붙여넣은 첨부입니다.
- `alwaysUpdateLinks: true` — Obsidian이 노트를 옮기면 링크를 자동 갱신합니다. 하지만 Claude가 파일 이름·경로를 바꿀 때는 자동 갱신이 없으므로, 이름 변경 시 `[[…]]` 참조를 직접 grep해서 함께 고쳐야 합니다.
- 활성 플러그인: `obsidian-git`, `obsidian-excalidraw-plugin`, `calendar`(daily notes), `block-link-plus`(블록/헤딩 링크), `to-word`, `translate`, `dynamic-outline`, `editing-toolbar`, `custom-font-loader`(나눔바른고딕), `css-editor`.

## Git 사용

`obsidian-git` 플러그인이 `vault backup: YYYY-MM-DD HH:mm:ss` 형식으로 자동 커밋합니다(예: `b2258c6`). 수동으로 커밋할 때는 이 자동 백업과 섞이지 않게 의미 있는 메시지를 쓰고, `.gitignore`가 없어서 `.obsidian/`과 `.trash/`까지 전부 추적된다는 점을 감안하세요 — `git add -A`는 플러그인 설정 변경이나 휴지통 파일까지 함께 커밋합니다.

경로에 한글과 공백이 모두 들어가므로(`OneDrive\바탕 화면\valut`) 셸에서 경로는 반드시 인용부호로 감싸고, `git ls-files` 출력의 이스케이프된 한글 파일명(`\352\265\220...`)은 `git -c core.quotepath=false ls-files`로 읽으면 그대로 볼 수 있습니다.
